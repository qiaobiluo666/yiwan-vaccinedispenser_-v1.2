<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.yiwan.vaccinedispenser.system.sys.dao.VacDrugRecordMapper">




    <select id="selectGroupedRecords" resultType="com.yiwan.vaccinedispenser.system.sys.data.zyc.InventoryReportData">
        SELECT
        product_name AS productName,
        COUNT(*) AS totalNum
        FROM
        vac_send_drug_record
        WHERE
        deleted = 0
        <if test="createTimeStart != null">
            AND create_time <![CDATA[ >= ]]> #{createTimeStart}
        </if>
        <if test="createTimeEnd != null">
            AND create_time <![CDATA[ <= ]]> #{createTimeEnd}
        </if>
        GROUP BY
        product_name
    </select>
    <select id="countTodayGroupedByProductId"
            resultType="com.yiwan.vaccinedispenser.system.sys.data.request.vac.DrugRecordRequest"
            parameterType="java.lang.String">
        SELECT
            vaccine_id AS vaccineId,
            product_No AS productNo,
            product_name AS productName,
            COUNT(*) AS totalNum
        FROM vac_drug_record
        WHERE deleted = 0
          AND DATE(create_time) = CURDATE()
          AND product_No = #{productNo}
        GROUP BY product_No, product_name, vaccine_id
            LIMIT 1
    </select>


</mapper>